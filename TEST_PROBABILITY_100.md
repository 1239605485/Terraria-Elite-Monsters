# EliteMonsters 1.3.1：100% 概率测试说明

本测试版将普通、专家、大师、传奇四档的精英怪生成概率都设置为 100%。传奇档只在天顶世界（Zenith/fixed-boi 特殊种子）启用，旅途/Creative 档保持普通属性。符合条件的敌怪会随机分为三档：普通精英名称为“精英·怪物”（白色），稀有精英为“稀有·怪物”（蓝色），传奇精英为“传奇·怪物”（紫色）。颜色由 `Main.MouseText` 的原版稀有度参数设置，不再把颜色代码写进名称；生命、伤害、防御、体型和金币奖励会随游戏模式和游戏进度提高。稀有精英额外随机掉落当前进度的原版道具；传奇精英免疫击退，击杀时必掉 1 个宝匣（困难模式前 70% 金匣、困难模式后 70% 钛金匣，30% 当前环境环境匣）。近战/冲锋型会按冷却瞬移到玩家侧后方，远程/法师型会横向换位，飞行型会蛇形追击，蠕虫型会斜向突袭，特殊型会规避移动；生命低于 35% 时触发一次伤害狂暴。

注意：这只代表每个符合条件的普通 NPC 都会尝试转化；友好 NPC、城镇 NPC、Boss、不可伤害、无敌、可捕捉和无效 NPC 仍会被过滤。由于所有普通敌怪都可能变成精英怪，测试时可能出现敌怪强度明显升高的情况。1.3.1 会优先直接 Hook `NPC.AI()`，并在 `NPC.NPCLoot` 后处理稀有奖励和传奇宝匣；日志中应看到 `Known NPC AI hook installed`、`NPC.NPCLoot reward hook installed` 和 `Item.NewItem reward API: found=1`。

测试完成后，将 `EliteMonsters/mod.c` 中的概率数组恢复为正式配置：

```c
static const int g_spawn_chance_percent[ELITE_MODE_COUNT] = {
    20, 30, 40, 50
};
```
