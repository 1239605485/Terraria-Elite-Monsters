# EliteMonsters 1.2.1 修复说明

## 问题

Terraria 使用固定的 NPC 对象池，并在敌怪死亡后复用相同的对象指针。1.2.0 把对象指针永久加入“已处理”和“已发奖”集合，导致同一槽位后续生成的 NPC 仍可能被识别为旧精英；如果旧生命周期已经执行过奖励判定，新的传奇精英死亡时会被 `already_rewarded` 提前拦截。

## 修复

- 不再把 NPC 对象指针永久标记为“已处理”。
- 每次 `NPC.SetDefaults` 完成时，先清除该对象上一个生命周期的精英、AI 和奖励状态。
- 对象池中的每个指针只占一个跟踪槽位；槽位复用时更新当前精英等级，不再无限追加旧生命周期。
- “已发奖”改为当前精英生命周期内的状态，并在新 NPC 生成时重置。

## 掉落规则

传奇精英仍有 30% 的独立概率掉落一个原版环境匣。修复的是“槽位复用后完全跳过判定”，不是把 30% 改为必掉；因此单次击杀未掉落仍属于正常结果。

## 日志核对

所附 2026-09-01 00:07:38 运行日志显示：KernelLoader 成功加载 1 个模组，并为本模组安装了 8 个 Hook（ID 4–11）；其中最后一个 Hook 与源码中的 `NPC.NPCLoot` 奖励 Hook 数量一致。日志没有导出模组自有的 `EliteMonsters` 标签消息，因此无法从该日志还原每次击杀的 30% 随机结果。
